include("${CMAKE_CURRENT_LIST_DIR}/@targets_export_name@.cmake")

include(CMakeFindDependencyMacro)

if("@ZLIB_FOUND@")
    find_dependency(ZLIB CONFIG)
endif()

if("@BZip2_FOUND@")
    find_dependency(BZip2 CONFIG)
endif()

if("@tomcrypt_FOUND@")
    find_dependency(tomcrypt CONFIG)
endif()
